
NEXT_PUBLIC_FIREBASE_API_KEY=""
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=""
NEXT_PUBLIC_FIREBASE_PROJECT_ID=""
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=""
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=""
NEXT_PUBLIC_FIREBASE_APP_ID=""
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=""

NEXT_PUBLIC_PAYPAL_CLIENT_ID=""
# For NEXT_PUBLIC_PAYPAL_FUNCTIONS_BASE_URL:
# Local emulation example: NEXT_PUBLIC_PAYPAL_FUNCTIONS_BASE_URL="http://127.0.0.1:5001/your-project-id/your-region/paypalAPI"
# Deployed function example: NEXT_PUBLIC_PAYPAL_FUNCTIONS_BASE_URL="https://your-region-your-project-id.cloudfunctions.net/paypalAPI"
NEXT_PUBLIC_PAYPAL_FUNCTIONS_BASE_URL=""


# Server-side only (DO NOT prefix with NEXT_PUBLIC_ and DO NOT commit real values for these to Git)
# These are for your Cloud Functions environment or local Node.js server if you build one.
# For Firebase Cloud Functions, set these using:
# firebase functions:config:set paypal.client_id="YOUR_SERVER_SIDE_PAYPAL_CLIENT_ID" (can be same as public if app is configured so)
# firebase functions:config:set paypal.client_secret="YOUR_PAYPAL_SECRET"
# firebase functions:config:set paypal.environment="sandbox_or_live"

# Example values for functions/.env (for local emulation, used by functions/src/paypalClient.ts if functions.config().paypal is not set)
# PAYPAL_CLIENT_ID="YOUR_SANDBOX_OR_LIVE_CLIENT_ID_FOR_SERVER_SDK"
# PAYPAL_CLIENT_SECRET="YOUR_SANDBOX_OR_LIVE_SECRET_FOR_SERVER_SDK"
# PAYPAL_ENVIRONMENT="sandbox" # or "live"

# For Firebase Admin SDK (server-side, e.g., in Cloud Functions or Next.js API routes if they run in a Node.js env)
# Option 1: Set GOOGLE_APPLICATION_CREDENTIALS environment variable to the path of your service account JSON file.
# Option 2: Store the content of the service account JSON file as a single-line string in an environment variable.
# FIREBASE_ADMIN_SERVICE_ACCOUNT_JSON_STRING='{"type": "service_account", ...}'
